#include <stdio.h>
#include "main.h"

/**
 * add - returns the sun of two number i, n
 * @n: one of numbers to add
 * @i: the other number to sum
 *
 * Return: int
 *
 * description: Adds two numbers
 */
int add(int i, int n)
{
	int total;

	total = i + n;

	return (total);
}
